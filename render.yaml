services:
  - type: web
    name: accessories-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:8000 app_enhanced:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
