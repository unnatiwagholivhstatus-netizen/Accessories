services:
  - type: web
    name: accessories-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -b 0.0.0.0:8000 -k uvicorn.workers.UvicornWorker app_enhanced:app
